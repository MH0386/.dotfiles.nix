# PROJECT KNOWLEDGE BASE

**Generated:** 2026-04-03
**Commit:** 8960f391133514abc09e5f810921e4ab94b19666
**Branch:** main

## OVERVIEW

NixOS dotfiles managed via Nix flakes + Home Manager.
No traditional shell dotfiles—shell/editor configs are declarative Nix modules.

## STRUCTURE

    .dotfiles/
    ├── flake.nix                 # Entry point (flakes)
    ├── devenv.nix                # Dev environment
    ├── .github/                  # CI workflows + Mergify
    └── nixos/
        ├── configuration/        # Shared NixOS modules
        ├── device/              # Per-host configs (Desktop/Laptop)
        └── home-manager/        # User-level dotfiles

## WHERE TO LOOK

| Task                | Location               | Notes                    |
| ------------------- | ---------------------- | ------------------------ |
| NixOS system config | `nixos/configuration/` | Shared modules           |
| Per-host overrides  | `nixos/device/`        | Desktop/Laptop specifics |
| Home Manager        | `nixos/home-manager/`  | Most content here        |

## CONVENTIONS

- **Auto-generated files**: Never edit `hardware-configuration.nix`
- **Modular**: Split by concern (boot, programs, services, etc.)
- **CI**: Pre-commit runs deadnix, nixfmt, actionlint, yamllint, etc.
- **Flakes**: All configs via `flake.nix` inputs, no channels

## ANTI-PATTERNS (THIS PROJECT)

- DO NOT manually edit any `hardware-configuration.nix`—auto-generated by NixOS
- DO NOT use channels—flakes only
- No `.bashrc`/`.zshrc` files—shell configured via Home Manager

## COMMANDS

    ```bash
    # Rebuild NixOS
    sudo nixos-rebuild switch --flake .#hostname

    # Update flakes
    nix flake update

    # Pre-commit manually
    pre-commit run --all-files
    ```

## NOTES

- Devenv integrated for reproducible dev shells
- Mergify auto-queues PRs on CI pass
- No traditional tests—CI validates via nix flake check
